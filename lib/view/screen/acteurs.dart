import 'package:flutter/material.dart';

class Acteurs extends StatelessWidget {

  const Acteurs({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Page des acteurs'),
    );
  }
}
